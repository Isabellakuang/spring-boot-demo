# FastAPI 核心
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# 向量数据库 - 仅使用免费的 ChromaDB
chromadb==0.4.22

# 本地嵌入模型 - 完全免费
sentence-transformers==2.3.1

# 基础依赖
numpy==1.26.3
python-multipart==0.0.6

# 日志
python-json-logger==2.0.7

# 注意：已移除所有需要付费API的依赖
# - openai (需要 OpenAI API key)
# - anthropic (需要 Anthropic API key)
# - pinecone-client (需要 Pinecone API key)
# - langchain (依赖付费LLM服务)
# - weaviate-client (可选，但我们只用ChromaDB)
